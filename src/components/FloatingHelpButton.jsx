function FloatingHelpButton({ onClick }) {
  return (
    <button type="button" className="floating-help-button" onClick={onClick}>
      Есть вопросы?
    </button>
  )
}

export default FloatingHelpButton
